<div class="placeholder-svg-aspect-ratio placeholder-svg-bg h-100">
    <div class="placeholder-frame h-100">
        {% if text != blank %}
            <span>{{ text }}</span>
        {% else %}
            <span>{{ width }}x{{ height }}</span>
        {% endif %}
    </div>
    {{ 'lifestyle-1' | placeholder_svg_tag: 'placeholder-svg' }}
</div>